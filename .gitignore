node_modules
dist
dist-ssr
*.local
*.log
.DS_Store
_scrape
_shots
.vite
.claude

# internal-only audit doc — never publish
PROJECT-CONTEXT.md

# responsive WebP variants — generated by scripts/gen-images.mjs, regenerated on
# every build via the prebuild hook, so no need to track them (12 MB of binaries).
# <Picture> falls back to the original JPG/PNG if a variant is ever missing.
public/media/img/*-[0-9]*.webp
